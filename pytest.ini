[tool:pytest]
minversion = 6.0
testpaths = tests
python_files = tests/*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    --cov=tests
    --cov-report=html
    --cov-report=term-missing
    --cov-fail-under=70
